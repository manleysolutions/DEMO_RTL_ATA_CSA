{
  "name": "usps-dashboard",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node server.mjs"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
